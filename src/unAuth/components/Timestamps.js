import React from "react";
import "./Timestamps.css";

const Timestamps = () => {
  return (
    <section className="timestamps">
      <div className="timestamps-container">
        <h2 className="timestamps-title">Timestamps</h2>
        <p className="timestamps-description">
          Manage and organize your video timestamps with ease.
        </p>
      </div>
    </section>
  );
};

export default Timestamps;

